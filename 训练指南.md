# 肺炎 X 光分类 — 训练指南

基于论文 *"Advanced pneumonia classification using transfer learning on chest X-ray data with EfficientNet and ResNet"* (TELKOMNIKA, 2025)。

---

## 项目结构

```
XrayPneumoniaDetection/
├── data/
│   ├── train/                  # 训练集 (5,216 images)
│   │   ├── NORMAL/
│   │   └── PNEUMONIA/
│   ├── val/                    # Holdout 测试集 (16 images)
│   │   ├── NORMAL/
│   │   └── PNEUMONIA/
│   └── shuffled_test/          # 推理集 (624 images, 无标签)
├── models/                     # 模型权重存放
├── results/                    # 输出曲线图、混淆矩阵、提交 CSV
├── utils/
│   ├── dataset.py              # 数据加载、预处理、增强
│   ├── model.py                # EfficientNetB3 + 自定义分类头
│   ├── evaluate.py             # 评估指标、绘图
│   └── grad_cam.py             # Grad-CAM 可视化
├── train.py                    # 训练入口
├── inference.py                # 推理入口
└── requirements.txt
```

---

## 环境安装

```bash
# PyTorch (根据你的 CUDA 版本选择)
pip install torch torchvision

# 其余依赖
pip install pillow numpy scikit-learn matplotlib opencv-python
```

---

## 数据分工

| 角色 | 数据来源 | 数量 | 用途 |
|------|----------|------|------|
| **Training** | `data/train/` 的 80% (stratified split) | ~4,172 | 梯度更新，模型学习 |
| **Validation** | `data/train/` 的 20% (stratified split) | ~1,044 | 监控训练、早停、选 checkpoint |
| **Holdout Test** | `data/val/` | 16 | 训练结束后最终评估一次 |
| **Inference** | `data/shuffled_test/` | 624 | 推理，输出提交 CSV |

> 训练集和验证集按类别分层抽样（stratify），保证每类比例一致。

---

## 数据预处理

| 步骤 | 训练集 | 验证/测试集 |
|------|--------|------------|
| 颜色通道 | 灰度转 RGB (3 channels) | 灰度转 RGB |
| 尺寸 | Resize 224×224 | Resize 224×224 |
| 增强 | RandomHorizontalFlip(0.5), Rotation(±10°), Affine(±5%), ColorJitter(0.1) | 无 |
| 归一化 | mean=0.5, std=0.5 (3通道均相同) | mean=0.5, std=0.5 |

---

## 模型架构

```
EfficientNetB3 (预训练, ImageNet, 冻结 backbone)
  └── features (全部冻结, ~10.7M params)
  └── classifier
      ├── Dropout(0.3)
      ├── Linear(1536 → 256)
      ├── ReLU
      ├── Dropout(0.3)
      └── Linear(256 → 2)
```

- **总参数量**: ~11,090,000
- **可训练参数 (Phase 1)**: ~394,000 (仅分类头)
- **Loss**: CrossEntropyLoss
- **Optimizer**: AdaMax (论文最佳)
- **Scheduler**: ReduceLROnPlateau (mode='max', factor=0.5, patience=3)

---

## 训练

### 基本命令

```bash
# 场景 1 训练（推荐起步）
python train.py --scenario 1

# 场景 2 训练
python train.py --scenario 2

# 场景 3 训练
python train.py --scenario 3

# 场景 1 + 微调 backbone 最后 3 个 block
python train.py --scenario 1 --unfreeze_after 3
```

### 三种预设场景

| 参数 | Scenario 1 | Scenario 2 | Scenario 3 |
|------|-----------|-----------|-----------|
| Optimizer | AdaMax | AdaMax | AdaMax |
| Learning Rate | 0.001 | 0.001 | 0.01 |
| Batch Size | 16 | 20 | 45 |
| Epochs | 20 | 30 | 30 |

### 训练流程

```
加载数据 → 构建模型 (冻结 backbone)
    ↓
Phase 1: 只训练分类头 (场景配置的 epochs)
    ↓
[可选] Phase 2: --unfreeze_after N → 解冻最后 N 个 block, lr 降为 1/10，再训练一轮
    ↓
Holdout Test: 在 data/val/ (16张) 上最终评估
```

### 训练输出

每次训练生成以下文件到 `results/` 和 `models/`：

| 文件 | 说明 |
|------|------|
| `models/efficientnetb3_scenario{N}.pth` | 最佳模型权重（按 val F1 选取） |
| `results/training_curves_s{N}.png` | train/val loss 和 accuracy 曲线 |
| `results/confusion_matrix_val_s{N}.png` | 验证集混淆矩阵 |
| `results/confusion_matrix_test_s{N}.png` | Holdout 测试集混淆矩阵 |

---

## 推理

```bash
python inference.py --model efficientnetb3_scenario1.pth

# 指定输出文件名和 batch size
python inference.py --model efficientnetb3_scenario1.pth --output submission.csv --batch_size 64
```

| 参数 | 默认值 | 说明 |
|------|--------|------|
| `--model` | `efficientnetb3_scenario1.pth` | 模型文件名 (`models/` 目录下) |
| `--output` | `submission_efficientnet.csv` | 输出 CSV 文件名 |
| `--batch_size` | 32 | 推理批次大小 |

输出格式：`id,TARGET`（与原有格式完全兼容）。

---

## 可调参数速查

### 场景配置 (`train.py:20-24`)

```python
SCENARIOS = {
    1: {'epochs': 20, 'lr': 0.001, 'batch_size': 16, 'optimizer': 'adamax'},
    # 可切换 optimizer: 'sgd' 或 'rmsprop'
}
```

### 训练控制 (`train.py`)

| 行号 | 参数 | 默认 | 说明 |
|------|------|------|------|
| ~101 | `scheduler patience` | 3 | 连续 N 轮不提升则衰减 lr |
| ~101 | `scheduler factor` | 0.5 | LR 衰减倍率 |
| ~101 | `scheduler min_lr` | 1e-7 | LR 下限 |
| ~107 | `early_stop patience` | 5 | 连续 N 轮不提升则早停 |
| ~207 | `fine-tune lr 倍率` | `lr * 0.1` | 微调阶段学习率 |

### 数据增强 (`utils/dataset.py`)

| 行号 | 参数 | 默认 | 说明 |
|------|------|------|------|
| ~19 | `RandomRotation` | 10° | 旋转范围 |
| ~20 | `RandomAffine translate` | (0.05, 0.05) | 平移范围 |
| ~21 | `ColorJitter(brightness, contrast)` | (0.1, 0.1) | 亮度/对比度抖动 |
| ~87 | `test_size` | 0.2 | train/val 划分比例 |

### 模型结构 (`utils/model.py`)

| 行号 | 参数 | 默认 | 说明 |
|------|------|------|------|
| ~18 | `Dropout rate` | 0.3 | 分类头 Dropout 概率 |
| ~19 | `hidden_dim` | 256 | 分类头隐层维度 |

---

## 常见问题

### 显存不足

减少 `batch_size`（在 `SCENARIOS` 字典中修改），或使用较小模型如 `efficientnet_b0`。

### 验证指标波动大

减小 LR（如 `0.001 → 0.0005`），或增大 `scheduler patience`。

### 过拟合

增大 `Dropout rate`（如 `0.3 → 0.5`），减少数据增强强度，或设置 `--unfreeze_after 0`（不做微调）。
